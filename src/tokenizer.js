function tokenize(source /* String */ ) {
    var isString = false
    var letter;
    for (let x = 0; x < source.length; x++) {        
        letter = source[x];
        if (letter == "\""):
            continue;
    }
}